#!/bin/bash

# Define the output file
OUTPUT_FILE="installed_packages.md"

# List all installed Arch Linux packages
pacman -Qq > "$OUTPUT_FILE"

echo "" >> "$OUTPUT_FILE"
echo "Package list saved to $OUTPUT_FILE"
