#!/usr/bin/env bash

rsync -avzhP --update --delete \
    --exclude '**/*[cC][aA][cC][hH][eE]*' \
    --exclude '.bun' \
    --exclude '.docker' \
    --exclude '.cache' \
    --exclude '.cargo' \
    --exclude '.mozilla' \
    --exclude 'node_modules' \
    --exclude 'target' \
    --exclude 'Unity' \
    /home/maneesh /media/veracrypt1/
